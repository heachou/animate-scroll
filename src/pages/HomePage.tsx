import { FC } from 'react';
import WebHeader from '../components/WebHeader';

export const HomePage: FC = (): JSX.Element => {

  return (
    <div className="">
      <WebHeader></WebHeader>
    </div>
  );
};